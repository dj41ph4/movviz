# dependencies
/node_modules
/engine/node_modules
/resolver/node_modules
/resolver/dist
/.pnp
.pnp.*

# engine runtime data
/.movviz-data
/engine/.movviz-data
/data
/dta

# next.js
/.next/
/out/
next-env.d.ts

# production / installers
/build
/dist
packaging/windows/installer/stage/
packaging/windows/installer/.tools/
packaging/windows/installer/movviz.ico

# local reference material — never shipped, never committed
/_refs/
/UX-CLAIR.md
/RECO-AGENT.md
/AGENTS.md
/AI.MD

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env*.local

# typescript
*.tsbuildinfo

# opencode agents (local only)
.opencode/agents/*.md

# AI assistant local config — never committed (no AI credit anywhere, project charter).
# No leading slash: must match at ANY depth, not just the repo root — a
# nested android-tv/.claude/ slipped past the old root-anchored pattern and
# got committed for real (2 files, confirmed live).
.claude/
/CLAUDE.md
.cursor/
.windsurf/
.cline/
.aider*
AGENTS.md
.opencode/

# debug logs
build-debug*.txt
build-errors.txt
build-output*.txt
engine/src/.movviz-data/
