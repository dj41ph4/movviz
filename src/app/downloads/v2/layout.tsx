import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Téléchargements",
};

export default function DownloadsV2Layout({ children }: { children: React.ReactNode }) {
  return children;
}
