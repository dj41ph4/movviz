import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Activité",
};

export default function ActivityV2Layout({ children }: { children: React.ReactNode }) {
  return children;
}
